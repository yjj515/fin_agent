# 更新日志

## 2025-01-11

### 修复
- 修复了模板中Jinja2语法错误，将复杂的Python表达式改为在Python代码中预处理数据
- 修复了模板中`run.get()`方法调用，改为直接访问字典键
- 添加了`win_rate_class`和`avg_return_class`字段，用于在模板中正确显示颜色

### 改进
- 改进了数据预处理逻辑，在Python代码中计算颜色类，而不是在模板中计算
- 增强了错误处理，确保即使数据缺失也能正常显示
